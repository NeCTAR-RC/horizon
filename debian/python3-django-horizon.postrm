#!/bin/sh

set -e

if [ "$1" = "remove" ] || [ "$1" = "disappear" ] ; then
	update-alternatives --remove openstack_dashboard /usr/lib/python3/dist-packages/openstack_dashboard
fi

#DEBHELPER#

exit 0
